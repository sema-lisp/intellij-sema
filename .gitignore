.gradle/
build/
.idea/
.intellijPlatform/
*.iml
out/

# PseudoScript-generated documentation output
target/

# Stray Node artifacts (this is a Gradle/Kotlin project, not a Node one)
node_modules/
package-lock.json

# Plugin signing secrets — never commit. Mirror these into GitHub Actions secrets for releases.
.secrets/
*.pem
*.p12
